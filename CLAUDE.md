# CLAUDE.md

The operating instructions for this repo live in [`AGENTS.md`](./AGENTS.md) — invariants,
commands, layout, conventions, versioning and the release process.

This file exists only so Claude Code finds them. It is intentionally a pointer rather than a
copy: two files drifting apart is worse than one file everyone reads.
