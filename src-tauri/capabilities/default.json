{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege surface for the deck window (NFR-3). Process spawning is not exposed to the webview at all — every git-wt invocation and PTY session goes through an allowlisted #[tauri::command] in Rust.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:default",
    "dialog:allow-open",
    "dialog:allow-message",
    "dialog:allow-confirm"
  ]
}
